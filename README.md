# Login Layout

Single-page React app showcasing a polished, responsive login form layout. This project is frontend-only and demonstrates modern UI/UX touches without any actual authentication logic.

## Setup

```bash
cd frontend
npm install
npm start
```

The app runs locally at `http://localhost:3000` and hits the React development server provided by `react-scripts`.
